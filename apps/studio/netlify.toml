[build]
  command = "pnpm install && pnpm run build:netlify:studio"
  publish = "apps/studio/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.33.2"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
