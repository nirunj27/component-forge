[build]
  command = "pnpm install && pnpm run build:netlify:playground"
  publish = "apps/playground/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.33.2"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
