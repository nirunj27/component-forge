name: Progress
description: Progress indicator
category: display

props:
  variant:
    type: enum
    values: [primary, secondary]
    default: primary
  disabled:
    type: boolean
    default: false

slots:
  - name: children
    required: false
    description: Content

a11y:
  role: status
