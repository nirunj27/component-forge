name: Radio
description: Single choice input
category: inputs

props:
  variant:
    type: enum
    values: [primary, secondary]
    default: primary
  disabled:
    type: boolean
    default: false

slots:
  - name: children
    required: false
    description: Content

a11y:
  role: status
