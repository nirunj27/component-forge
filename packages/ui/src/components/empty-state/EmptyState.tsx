import React from "react";
import styles from "./EmptyState.module.css";

export interface EmptyStateProps {
  variant?: "default" | "bordered" | "striped";
  loading?: boolean;
  empty?: boolean;
  /** Row or cell content */
  children?: React.ReactNode;
  /** Header area */
  header?: React.ReactNode;
  /** Footer area */
  footer?: React.ReactNode;
  onClick?: (event: React.MouseEvent<HTMLElement>) => void;
}

/**
 * No-data placeholder
 * @generated by Component Forge — edit schema and re-run forge generate
 */
export function EmptyState({ variant = "default", loading = false, empty = false, children, header, footer, onClick }: EmptyStateProps) {

  return (
    
      <button
      className={[styles.root, variant && styles[variant]].filter(Boolean).join(" ")} data-testid="empty-state"
      type="button"
      onClick={onClick}
      aria-busy={loading}
    >
      {children}
    </button>
    
  );
}

EmptyState.displayName = "EmptyState";
