import React from "react";
import styles from "./ScrollToTop.module.css";

export interface ScrollToTopProps {
  /** Visual style */
  variant?: "primary" | "secondary" | "ghost" | "danger";
  size?: "sm" | "md" | "lg";
  disabled?: boolean;
  loading?: boolean;
  /** Button label */
  children?: React.ReactNode;
  onClick?: (event: React.MouseEvent<HTMLElement>) => void;
}

/**
 * Back-to-top button
 * @generated by Component Forge — edit schema and re-run forge generate
 */
export function ScrollToTop({ variant = "primary", size = "md", disabled = false, loading = false, children, onClick }: ScrollToTopProps) {

  const handleKeyDown = (event: React.KeyboardEvent<HTMLElement>) => {
    if (["Enter", " "].includes(event.key) && onClick) {
      event.preventDefault();
      onClick(event as unknown as React.MouseEvent<HTMLElement>);
    }
  };
  return (
    
      <button
      className={[styles.root, variant && styles[variant], size && styles[size]].filter(Boolean).join(" ")} data-testid="scroll-to-top"
      type="button"
      onClick={onClick}
      disabled={disabled}
      aria-busy={loading}
      onKeyDown={handleKeyDown}
    >
      {children}
    </button>
    
  );
}

ScrollToTop.displayName = "ScrollToTop";
