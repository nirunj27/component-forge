import React from "react";
import styles from "./KeyValueDisplay.module.css";

export interface KeyValueDisplayProps {
  variant?: "default" | "bordered" | "striped";
  loading?: boolean;
  empty?: boolean;
  /** Row or cell content */
  children?: React.ReactNode;
  /** Header area */
  header?: React.ReactNode;
  /** Footer area */
  footer?: React.ReactNode;
  onClick?: (event: React.MouseEvent<HTMLElement>) => void;
}

/**
 * Label/value pairs
 * @generated by Component Forge — edit schema and re-run forge generate
 */
export function KeyValueDisplay({ variant = "default", loading = false, empty = false, children, header, footer, onClick }: KeyValueDisplayProps) {

  return (
    
      <button
      className={[styles.root, variant && styles[variant]].filter(Boolean).join(" ")} data-testid="key-value-display"
      type="button"
      onClick={onClick}
      aria-busy={loading}
    >
      {children}
    </button>
    
  );
}

KeyValueDisplay.displayName = "KeyValueDisplay";
