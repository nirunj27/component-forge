# LazyImageLoader

Image with lazy load placeholder

> Generated by [Component Forge](https://github.com) — edit the schema and re-run `forge generate`.

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `src` | `string` | — | Media source URL |
| `alt` | `string` | — | Accessible description |
| `loading` | `boolean` | false | — |

## Accessibility

- Role: `region`



## Usage

```tsx
import { LazyImageLoader } from "@component-forge/ui/lazy-image-loader";

<LazyImageLoader>Content</${schema.name}>
```
