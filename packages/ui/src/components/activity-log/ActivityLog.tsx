import React from "react";
import styles from "./ActivityLog.module.css";

export interface ActivityLogProps {
  variant?: "default" | "bordered" | "striped";
  loading?: boolean;
  empty?: boolean;
  /** Row or cell content */
  children?: React.ReactNode;
  /** Header area */
  header?: React.ReactNode;
  /** Footer area */
  footer?: React.ReactNode;
  onClick?: (event: React.MouseEvent<HTMLElement>) => void;
}

/**
 * Admin activity log table
 * @generated by Component Forge — edit schema and re-run forge generate
 */
export function ActivityLog({ variant = "default", loading = false, empty = false, children, header, footer, onClick }: ActivityLogProps) {

  return (
    
      <button
      className={[styles.root, variant && styles[variant]].filter(Boolean).join(" ")} data-testid="activity-log"
      type="button"
      onClick={onClick}
      aria-busy={loading}
    >
      {children}
    </button>
    
  );
}

ActivityLog.displayName = "ActivityLog";
