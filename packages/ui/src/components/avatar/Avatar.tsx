import React from "react";
import styles from "./Avatar.module.css";

export interface AvatarProps {
  variant?: "default" | "bordered" | "striped";
  loading?: boolean;
  empty?: boolean;
  /** Row or cell content */
  children?: React.ReactNode;
  /** Header area */
  header?: React.ReactNode;
  /** Footer area */
  footer?: React.ReactNode;
  onClick?: (event: React.MouseEvent<HTMLElement>) => void;
}

/**
 * User avatar image
 * @generated by Component Forge — edit schema and re-run forge generate
 */
export function Avatar({ variant = "default", loading = false, empty = false, children, header, footer, onClick }: AvatarProps) {

  return (
    
      <button
      className={[styles.root, variant && styles[variant]].filter(Boolean).join(" ")} data-testid="avatar"
      type="button"
      onClick={onClick}
      aria-busy={loading}
    >
      {children}
    </button>
    
  );
}

Avatar.displayName = "Avatar";
