import React from "react";
import styles from "./Button.module.css";

export interface ButtonProps {
  /** Visual style */
  variant?: "primary" | "secondary" | "ghost" | "danger";
  size?: "sm" | "md" | "jjjjjjj";
  disabled?: boolean;
  loading?: boolean;
  /** Button label */
  children?: React.ReactNode;
  onClick?: (event: React.MouseEvent<HTMLElement>) => void;
}

/**
 * Generic configurable button
 * @generated by Component Forge — edit schema and re-run forge generate
 */
export function Button({ variant = "primary", size = "md", disabled = false, loading = false, children, onClick }: ButtonProps) {

  const handleKeyDown = (event: React.KeyboardEvent<HTMLElement>) => {
    if (["Enter", " "].includes(event.key) && onClick) {
      event.preventDefault();
      onClick(event as unknown as React.MouseEvent<HTMLElement>);
    }
  };
  return (
    
      <button
      className={[styles.root, variant && styles[variant], size && styles[size]].filter(Boolean).join(" ")} data-testid="button"
      type="button"
      onClick={onClick}
      disabled={disabled}
      aria-busy={loading}
      onKeyDown={handleKeyDown}
    >
      {children}
    </button>
    
  );
}

Button.displayName = "Button";
